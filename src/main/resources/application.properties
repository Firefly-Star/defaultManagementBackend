spring.application.name=Backend

# Database
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/${DB_NAME:defaultmanagement}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:wxzWXZ1799824178}

# SQL init (auto-create tables on fresh DB)
spring.sql.init.mode=ALWAYS
spring.sql.init.continue-on-error=true

# JWT
jwt.secret=${JWT_SECRET:Firefly}
jwt.expire=${JWT_EXPIRE:3600000}

# CORS
cors.allowed-origins=${CORS_ORIGINS:http://localhost:8081}

# Server
server.port=${PORT:8080}
